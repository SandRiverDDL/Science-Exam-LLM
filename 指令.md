基于 设计.md为我完成项目，有以下要求：
1.使用config.yaml管理参数
2.目前只有 jinaai/jina-embeddings-v4-text-retrieval-GGUF和jinaai/jina-reranker-v3-GGUF 模型，其他embedding模型后续再添加，也就是说目前只有embedding只有一路
3.faiss使用IndexFlatIP + float16 存储
4.使用BM25辅助embedding找回，但不作为主力检索
5.不同检索方式实现模块化，因为我要通过实验比较不同检索方式的效果
6.我需要先检验Qwen3-8B-Instruct-FPQuant-QAT-MXFP4-TEMP能否正常输出文字，然后用它做zero-shot分类，每次把问题和一个回答输入，只输出 0 或 1，0表示回答错误，1表示回答正确，注意KV cache，每次的问题是相同的，zero-shot作为base，跟后续实验做对比

构造一个流程，使得zero shot的qwen3模型，从train.csv读取问题和选项，像sample_submission.csv一样输出文件到output文件夹，同时拿预测结果和train.csv的answer做对比，并输出准确率，
这是单选题，
注意模块化，
prompt可以参考，你也可以改进，取决于你自己
You are a system that outputs ONLY one of A, B, C, D, E.
No explanation. No reasoning. Output must be exactly one letter.

Question:
{question}

Options:
A: ...
B: ...
C: ...
D: ...
E: ...

Answer (A/B/C/D/E):

set PYTHONPATH=%PYTHONPATH%;.\src
$env:PYTHONPATH="$env:PYTHONPATH;.\src"
$env:HUGGINGFACE_HUB_CACHE="D:\4000_projects\1project\LLM\data"

1. 加载训练集、验证集、测试集 CSV

   * 字段包括：question、options(A~E)、context 或背景文本（如果有）、correct_answer（训练集）

2.文本分块（chunking）
文本内容来自 data\raw\articles
对所有原始文档进行 chunk 处理：

chunk_size = 256 tokens
chunk_overlap = 50 tokens

操作方法：

使用 langchain.text_splitter.RecursiveCharacterTextSplitter 或等价自定义 splitter

分割每段文本为 token 级别的 chunks

每个 chunk 附带 metadata：

chunk_id

doc_id

position（chunk 的顺序）

3. 对所有 chunks 生成三种 embedding：

   * Jina-embeddings-v4 (text-retrieval)
   * BGE-m3 或 bge-large-v1.5
   * e5-mistral-instruct
   具体来说multilingual-e5-large-instruct Alibaba-NLP/gte-multilingual-base，且这两者我已经下载好了，注意Jina-embeddings-v4是主力

4. 保存 embedding 到 FAISS 索引

   * 对每个 embedding 分别创建一个 IndexFlatIP (FP16)
   * 保存 id→chunk_text、id→metadata 映射


使用qwen3的tokenizer而不是你自己编写的，可以参考以下实现
---

def tokenize_chunks(text, tokenizer, chunk_size=256, overlap=50):
    tokens = tokenizer.encode(text, add_special_tokens=False)
    n = len(tokens)
    chunks = []
    start = 0
    position = 0

    while start < n:
        end = min(start + chunk_size, n)
        chunk = tokens[start:end]
        chunk_text = tokenizer.decode(chunk, skip_special_tokens=True)

        chunks.append({
            "text": chunk_text,
            "position": position,
        })

        if end == n:
            break
        start = end - overlap
        position += 1

    return chunks

    对某个csv文件分块时，要打印 “正在分块文件：文件名”例如data\raw\articles\3.csv
    embedding部分加上e5 和bge multilingual-e5-large-instruct Alibaba-NLP/gte-multilingual-base
    我的csv文件总大小为17G，你自己分析，我32G（大约25G）内存空闲是否够用，如果不够用可能需要一个文件一个文件处理

    暂时放弃jina（但不要删除相关代码），因为jina参数太大
    同时引入bge-m3模型作为辅助检索模型，现在的检索主力模型是[multilingual-e5-large-instruct](https://huggingface.co/intfloat/multilingual-e5-large-instruct)
    ✔ 主力模型：multilingual-e5-large-instruct（最稳 → 决定你得分上限）
    ✔ 辅助模型：bge-m3（最佳互补 → 召回提升最大）
    ✔ 第三：gte-multilingual-base

我感觉非常慢，这正常吗，我决定先只启动gte去生成向量，快速验证，并检查以下问题：
你可能在用 CPU（GGUF）或单线程 tokenization → 会把预计速度砍到 1/10 或更低。

没有批量化（batch=1 或极小 batch） → 实际吞吐率急剧下降。

tokenizer 单线程或 Python-level decode/encode 成为瓶颈（尤其用 whitespace 切分或 slow tokenizer）。

I/O 磁盘读取慢（大量小文件） → CPU 等待 I/O。

没有把模型放到 GPU（或显存不足导致频繁 CPU-GPU 交换）。

框架/实现问题：例如用 sentence-transformers 但没有启用 device='cuda' 或没使用 torch.inference_mode()。

并行进程未启用（建议 tokenization 与 model inference 分离并行）


程序出bug，似乎是提示说超出了yield的limit，我没有报错错误报告

当程序 print 正在分块文件：data\raw\articles\1.csv\1.csv，到底在做什么，是在embedding吗？总不能分块分这么久吧
能不能实时显示embedding的进度，比如已经处理了多少个chunk，多少MB文件（估计）？

1.目前只用gte模型，除此之外，参考这个文档进行进行加速，因为每秒100chunks实在太慢了
2.为什么我跑了这么多我看不到embedding向量的输出？

1.[progress] flush=68 chunks=13056 bytes~11.00MB speed=265.5 chunks/s这个速度还是只有265chunks，而且我batchsize定多设定成192，再高就会超出显存，而且192显示的speed没有比64更快，而且我体感感觉64反而打印得更快，虽然显示的速度差不多
2.我希望这一段print控制打印频率，大概5秒打印一次，而且打印会覆盖上一次的打印，而不是每次都打印新的一行，例如这样
def print_and_overwrite(text):
    """
    打印文本，并用 \r 将光标移回行首，用于覆盖上次输出。
    """
    # 1. 打印文本
    # 2. 使用 end='\r' 来代替默认的 '\n'
    # 3. 如果需要清除残留，可以先打印足够多的空格
    print(f"\r{text}", end="")

    [progress] flush=50 chunks=9600 bytes~12.98MB speed=168.3 chunks/sTraceback (most recent call last):
    既然瓶颈在CPU侧，那能否离线将文档全转化为token，然后再输入embedding模型？
    我增大了一倍chunk_size_tokens，但目前的speed*2和之前差不多 265.5 chunks/s 

    [progress] flush=70 chunks=13440 bytes~18.60MB speed=166.0 chunks/s 怎么没有什么区别？


    multilingual-e5-large-instruct Alibaba-NLP/gte-multilingual-base BGE-m3

我决定新增BAAI/bge-small-en-v1.5模型以快速生成embedding，你自己决定是在bge-m3基础上新增还是直接新增一个索引


问题1：le "d:\4000_projects\1project\LLM\src\pipeline\build_chunks_and_indices.py", line 325, in main   
    result = build_indices_streaming(
  File "d:\4000_projects\1project\LLM\src\pipeline\build_chunks_and_indices.py", line 252, in build_indices_streaming
    flush()
  File "d:\4000_projects\1project\LLM\src\pipeline\build_chunks_and_indices.py", line 209, in flush  
    if use_token_ids and buf_ids and hasattr(bge_small_emb, "embed_from_ids"):
UnboundLocalError: local variable 'buf_ids' referenced before assignment
问题2：CSV迭代异常 data/raw/articles\4.csv\4.csv: field larger than field limit (131072)

我觉得bge small也不小，所以切换到bge-m3模型了，但
1.怎么这么慢，才50chunks每秒
2.Token indices sequence length is longer than the specified maximum sequence length for this model (9529 > 8192)这个错误怎么来的？
Loaded text docs: 0 from data/raw/articles
Found CSV files: 38 from data/raw/articles
`torch_dtype` is deprecated! Use `dtype` instead!
[init] BGE device: cuda
[tokenize] 使用 BGE 的 tokenizer 进行分块（直通 token ids）
正在分块CSV：data\raw\articles\0.csv\0.csv
Token indices sequence length is longer than the specified maximum sequence length for this model (9529 > 8192). Running this sequence through the model will result in indexing errors
[progress] flush=3 chunks=384 bytes~0.08MB speed=51.4 chunks/sTraceback

1.我准备就长期使用bge-small了，还有什么加速embedding的方法吗？
2.我希望实现每隔多少个chunk就生成一次索引（大概十分钟一次？），并设置检查点，支持中断继续

参考以下，本项目可以用来加速embedding吗
SentenceTransformers + FastTokenizer + multi-worker dataloader
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("BAAI/bge-small-en-v1.5", 
                            device="cuda",
                            trust_remote_code=True)

embeddings = model.encode(
    docs,
    batch_size=4096,          # 发疯一样的大 batch
    device="cuda",
    show_progress_bar=True,
    normalize_embeddings=True,
    convert_to_numpy=True,
    num_workers=8             # tokenizer 多线程

)

我添加了一个新的embedding模型Qwen/Qwen3-Embedding-0.6B, 替我加入代码
我想知道是否可以用它来加速embedding

为什么qwen3 0.6b这么慢，才[progress] flush=3 chunks=384 bytes~0.08MB speed=17.6 chunks/s
是不是因为什么bug？
是不是没有启动flashattention

- 确保在 GPU + FP16：
- embedding_qwen3.device: "cuda"
- embedding_qwen3.dtype: float16
- 开启直通 token-id（避免重复分词）：
- pipeline.embed_from_tokens: true
- pipeline.embed_tokens_model: qwen3
- 看日志应出现： [tokenize] 使用 Qwen3 的 tokenizer 进行分块（直通 token ids）
- 平衡序列长度与分块大小：
- 将 embedding_qwen3.max_length: 256 （或略高于你的 chunk_size_tokens ，例如 256/320），避免 512 的无谓填充。
- 调整批量：
- 起步用 pipeline.batch_size: 256 或 512 ，有 16–24GB 显存可试 1024 ；OOM 就回退。
- 让 PyTorch 更快：
- CUDA 上默认已用 AMP（自动混精）。可额外允许 TF32：在入口脚本加 torch.backends.cuda.matmul.allow_tf32 = True （FP16 已足够的话，这项影响不大）。
- 如果只想“更快更小”：
- 直接用 bge-small-en-v1.5 在 GPU，吞吐通常比 Qwen3 0.6B 更高。

use_fast

University of California-Los Angeles (Los Angeles, CA)
Harry
Smith
2004 7 7

目前我使用bge-small的embedding速度大概为600 chunks每秒，要embedding 17G的数据需要十几小时，帮我分析以下我的程序的速度瓶颈在哪里，可以采取哪些措施

实施这些优化,但需要注意我是windows系统，使用多进程解码可能会卡主，
另外在build_chunks_and_indices.py中可以尝试重构，这个文件太大了

1.build_chunks_and_indices.py的文件还是太大了可能需要重构
2.[progress] flush=22 chunks=45056 bytes~52.19MB speed=854.2 chunks/s emb=51% io=2%
这个速度和时间占比怎么样？ emb + io加起来也才53%
3.你确定embedding_builder.py和index_manager.py适合放在pipeline目录下，而不是 index或者retrievel目录下
4.我准备暂时改成一分钟保存一次，然后（1）检查中断后能否真的再次加载
（2）帮我写一个脚本，在保存的faiss向量里检索0.csv里的text内容，看看保存的向量是否真的有效

1.csv0数据的在 'data/raw/articles/0.csv/0.csv',你的脚本中的列名似乎有问题
[样本 7] 查询文本 (前100字): 0X or 0-X ("zero/oh ex") may refer to:...
[检索结果] Top-5:
  [1] 距离=166672.0000 | chunk_id=0.8036060333251953 | doc_id=0.csv:row:0
      文本片段: 0 ( zero ) is a number representing an empty quantity. adding 0 to any number le...
  [2] 距离=44025.0000 | chunk_id=0.7438042759895325 | doc_id=0.csv:row:0
      文本片段: 0 ( zero ) is a number representing an empty quantity. adding 0 to any number le...
  [3] 距离=165743.0000 | chunk_id=0.681257426738739 | doc_id=0.csv:row:0
      文本片段: 0 ( zero ) is a number representing an empty quantity. adding 0 to any number le...
  [4] 距离=44508.0000 | chunk_id=0.662455677986145 | doc_id=0.csv:row:0
      文本片段: 0 ( zero ) is a number representing an empty quantity. adding 0 to any number le...
  [5] 距离=152025.0000 | chunk_id=0.6491485238075256 | doc_id=0.csv:row:0
      文本片段: 0 ( zero ) is a number representing an empty quantity. adding 0 to any number le...
这种检索结果正常吗
2.[progress] flush=6 chunks=12288 bytes~12.74MB speed=864.7 chunks/s emb=54% io=3%
断点重续之后，chunks和bytes只显示的是本次执行的embedding的chunks，我希望还要显示的是累计的chunks和bytes

[checkpoint] saved at chunks=38912 -> data/faiss/checkpoints/embedding_progress.json
D:/6000_env/miniconda3/envs/llm/python.exe d:/4000_projects/1project/LLM/src/pipeline/build_chunks_and_indices.py

D:/6000_env/miniconda3/envs/llm/python.exe d:/4000_projects/1project/LLM/src/pipeline/verify_index.py

我看到embedding_progress.json中"chunk_id": 38912, "flush_count": 19,但bge_small_fp16_ip.faiss文件有200MB，这是不是数据上不一致了
我再次运行embedding时，似乎分块速度只剩600chunks每秒了，为什么还变慢了？
既然我已经采用tokenizer捷径了，那我预先tokenizer所有csv数据，那是不是会更快？

我需要你做出改动，每个chunk前都要加上该chunk所属文档的title
如何拼标题？用什么符号最合适？
⚡ 最推荐（业界公认最佳）：
格式："{title}\n\n{chunk_text}"

也就是：

标题
<空行>
chunk 内容

为什么用换行？

Tokenizer 对换行有天然分隔效果

不引入额外意义符号（避免语义误导）

模型会认为“换行 = 新段落”

高质量 embedding 常用格式（OpenAI, Cohere, Voyage）

目前我已经确定使用 复用token ids的trick了，因为多路embedding的耗时太长，我不能接受。另外我要求你使用以下trick
1.

总流程
【阶段 0：预处理】
- 文档级 embedding（doc-emb）
- 文档级实体提取（doc-entity）
- 动态 chunking + chunk embedding（chunk-emb）
- metadata 写入 parquet

【阶段 1：query 处理】
- query embedding
- query entity（NER）

【阶段 2：文档级召回（fast）】
top_docs = ANN(doc_emb, top 1000)

【阶段 3：文档实体重排序（cheap）】
for doc in top_docs:
    doc.score = 0.8 * embedding_score + 0.2 * entity_overlap

select doc_top_k = top 500–800

【阶段 4：chunk-level 召回】
从这 500–800 文档的 chunk 中 ANN 召回 1000–2000 chunk（由 bge-small 处理）

【阶段 5：二阶段精检】
用 Jina V4 计算 query 与 chunk 精确 embedding 相似度
选 top 100–200 进入 Reranker / LLM

【阶段 6：LLM rerank + answer】





## 0) 数据清洗（必做，高影响）以及预Tokenization 

目标：去掉垃圾、统一风格、减少无用 chunks。

**要点 / 操作（优先级高）**

1. **语言检测**（只保留目标语言en）

   * 用 `langdetect` 或 `fasttext`。
2. **只保留 ASCII**（去 emoji、罕见 Unicode）

   * regex: `re.sub(r'[^\p{Han}\p{Hiragana}\p{Katakana}\p{Hangul}\p{ASCII}]', '', text)` （需 `regex` 库）
3. **移除 URL / HTML / 控制字符**

   * URL regex、`BeautifulSoup` 去 html。
4. **Unicode 标准化**。
*   **代码**：`import unicodedata; text = unicodedata.normalize('NFKC', text)`
*   **作用**：解决全角/半角字符混乱（如 `１２３` vs `123`，`Ａ` vs `A`），以及不同的空格符号。这对于 Embedding 对齐非常重要。
5. 将全部文档text使用bge_small tokenizer预Tokenization 

6. **短文本过滤**（大多数噪音发生于极短文本）

   * `min_text_tokens = 32`（推荐）



---

## 1) 标题处理（Title cleaning）——你特别关心的部分

**目标**：只拼接“高信噪比”标题，并且限制长度，避免污染 embedding。

**步骤**

1. 标题噪声检测（核心）** — 决定是否拼接标题

   * 判定规则（示例）：

     ```python
     def is_good_title(t):
         t=t.strip()
         if not t or len(t)<3 or len(t)>120: return False
         alnum_ratio = sum(c.isalnum() for c in t)/max(1,len(t))
         if alnum_ratio>0.8: return False
         if " " not in t and len(t)>6: return False
         if re.search(r'(file_|doc_|id_|^v\d+\.)', t.lower()): return False
         return True
     ```
   <!-- * 额外：统计 title 中的英文单词比中文字符比率，低于阈值就判为垃圾。 -->
2. 去停用词（可选，仅当 title 很长时）

   * English stopword: NLTK/stopwords
   * Chinese: 自定义停用词表
   * 示例（Python）：

     ```python
     from nltk.corpus import stopwords
     stops = set(stopwords.words('english'))
     def shorten_title(t, max_words=8):
         words=t.split()
         words=[w for w in words if w.lower() not in stops]
         return " ".join(words[:max_words])
     ```
3. 限长（token级）：

   * `title_max_tokens = 8~32`（推荐 8-16 英文单词 / 16–32 token 视模型）
   * 用 tokenizer 截断：`title_tokens = tokenizer.encode(title)[:title_max_tokens]`
4. 正则清洗（移除常见噪音）：

   * remove serials: `re.sub(r'\b[A-Z0-9_-]{6,}\b','',title)`
   * remove leading/trailing punctuation: `title.strip(" -_:;.,")`

**拼接建议**

* 如果 `is_good_title` 为 True：

  ```
  chunk_text_final = title_clean + "\n\n" + entities_line + "\n\n" + chunk_body
  ```
* 否则，不拼 title。

---
处理后把结果写到 Parquet（列：doc_id, title, doc_ids, length_tokens）以便后续重用。每隔大概256MB生成一次 1.Parquet   2.Parquet
实现以上要求，代码可以考虑放在processing文件夹下，如果你有需要生成新的文件夹也可以

我已经安装了beautifulsoup4 regex langdetect，不要你自己去实现，用regex去移除url
标题处理建议里有一个是错的，他提到直接用字符估计token数，我不需要，直接encode并标题的ids，后续直接考虑合并
基于两个建议文件，修改代码，注意，我准备进一步将动态chunking和预tokenize结合，在一个pipline中完成从csv文件 到 chunks，具体怎么做我会后续告诉你，但你考虑预留接口位置

将chunking的流程并入 preprocess_data，注意采用父文档索引，子文档chunksize 128，无overlap，父文档512
meta data里需要存储
rerank_text	String: 已拼接 Title。 Reranker 输入。 Reranker 需要文本来做 Cross-Attention 对比。
doc_id
start_idx, end_idx 父文档在原文档ids中的起始和终止index
chunk_id	Int/String	向量索引 ID	主键。
chunk_len	Int	子 Chunk 长度
这意味着确实需要把清洗结果结果写到 Parquet（列：doc_id, title, doc_ids, length_tokens）
这样后续才能根据 doc_id, start_idx, end_idx 快速定位到父文档

1.这个终端提示的超出token数会有什么问题吗？
2.我好奇你在partque里存储ids是以什么格式，我听说 np.uint16可以压缩空间，还可以指定压缩算法ZSTD： 速度较快，压缩率最高（推荐）。
3.我需要你在preprocess_data实现 断点重续机制

已处理 3,000 条文档，保留 526 条这种信息，后一天覆盖前一条，不然太长了
你的tokenize逻辑好像有问题啊，Token indices sequence length is longer than the specified maximum sequence length for this model (5947 > 512). Running this sequence through the model will result in indexing errors
或者说这只是一个警告
断点重续时，记得 “保留 3,381 条”这个数量要和之前的累加

写一个脚本，查看现有的 docs_1.parquet chunks_1.parquet里面存的是什么数据，叫做 check_parquet_data
我发现了一个很严重的问题，似乎你断点重续之后会重新开始写入chunks_1.parquet，而且重续之后还是从0开始计算 “保留 x 条”

你通过 len(tokens)*4这种方法估计完全是错的，首先我改成了 uint16后占据两字节，其次还有 zstd压缩，
现在我设定写入的阈值是 1G
然后你需要根据实际文件大小和处理文档行数来估算文件大小，一个chunks.parquet约为82MB，一个docs.parquet约为95MB

怎么连1Gchunk doc都输出不了就内存不够了，是不是意味着1G太大应该改成512MB,还是说你的程序或计算公式哪里有问题
注：ratio我后来微调了一点，但应该不影响


## 5) Embedding（生成向量）

**原则**

* 使用 HF backend + `embed_from_tokens` 可获得最高速度（前提：模型支持直接 id 输入）
* dtype：`float16`，device=`cuda`
* batch_size：尽量大（受显存限制，常见 1024~4096）
* 生成并保存：`id -> vector` 写入 FAISS（按批写入）

---

## 6) FAISS / Index（检索）

**结构**

* FAISS index: 存向量 + id
* metadata store (Parquet/SQLite/duckDB): id -> chunk_text, title

**Index 建议**

* IVF-FLAT 或 HNSW + 存储原向量（依规模）
* 建 index 时归一化（L2/cosine）: `index = faiss.IndexFlatIP(...)` with normalized vectors
* 写入时 batch commit + periodic persist
我之前也有embedding代码，不过那个代码可能要废弃了，现在我只用bge small进行embedding，而且直接从parquet读取chunk ids进行embedding

faiss索引只存储chunk_id和embedding，其他metadata通过chunkid从chunks的parquet文件中读取
faiss索引通过LZ4 算法压缩

1.这是什么问题，处理了400万向量后突然中止了 
2.我想在“已处理 4,009,984 chunks | 已嵌入 4,009,984 向量 | 速度 637 vec/s”打印信息中，按 hh:mm:ss显示当前时间
3.而且速度怎么越来越慢了，从1500 vec/s到637vecs/s

请回答.1.修改代码之后，我还能断点再续吗，还是要彻底重新开始 2.每60s自动保存间隔太短，改为5分钟保存一次 3.请回答向量是以fp16形式存储吗？还是fp32

总共应该由三千万向量，那我应该使用什么索引结构 ，我想使用IVFScalarQuantizer，使用fp16储存向量

我发现一个很严重的问题，1.csv像这样的文件里储存的wiki数据好像可能不是科学主题的，帮我写一个脚本，读取这个文件，可以调用build_chunks_and_indices.py里的清洗方法，筛选出长度大于32字符的，打印出前20行

我新加了一个数据集，例如'data\raw\archive\0_to_25000.parquet'，用同样的方法处理，可能只是读取的方法不一样，注意parquet力数据的列名我不确定

由于之前的csv文档数据里很多不是科学主题的，所以我换了一个大概几百MB的parquet格式的数据集，例如 'data\raw\archive\0_to_25000.parquet',文件包含的所有列: ['text', 'url', 'title'],但我希望你能保留对csv文件的读取能力防止未来我又切换数据集了，即实现主要代码逻辑和读取文件形式的解耦
注意，由于数据量大大减少，我不再采用 复用ids的技巧，这意味着新生成的 chunk.parquet 里
由于要使用多个embedding model进行ensemble，意味着要实现chunk和tokenizer的解耦，可以采取如下方式，tokenizer采用 tiktoken
父 512 / 子 128 token 的实现方法
🟩 方法 1（推荐）：用“句子列表 + 滑动窗口”实现 token 限制（动态的）

流程：

Step 1：先把文档按句子切（基于字符），得到：
sentence[i]  # 每个句子用 char_start/char_end 定义

Step 2：动态组合句子直到达到约 128 tokens，形成子 chunk

你可以这样做：

current_chunk = ""
for sent in sentences:
    if tokenizer(current_chunk + sent).len() > 128:
        yield current_chunk
        current_chunk = sent
    else:
        current_chunk += " " + sent

Step 3：父 chunk = 拼接 N 个子 chunk，直到约 512 tokens
注意

所有 chunk 都用 字符 offset 管理文本，不依赖 token offset。

embedding 模型 tokenize 时自动适配。

有问题啊
1.你确定使用了text_cleaner.py和title_cleaner.py定义的方法来清晰 text title吗
2.parquet文件的数据里，每行数据似乎没有id，这要求你自行生成id
3.我认为chunk数据里不用存 rerank_text，而且要存 title
4.你确定你保存了清洗过了的doc吗，对原parquet数据清洗之后，加上doc_id（如果原来没有id的话），再完全可以一起储存成一个文件（因为总共才几百MB）

处理完成，正在保存文件...
✅ 文档已保存: data/processed\documents_cleaned.parquet
   大小: 315.52 MB | 记录数: 130,926
✅ Chunks已保存: data/processed\chunks.parquet
   大小: 23.67 MB | 记录数: 1,281,646
✅ 统计信息已保存: data/processed\preprocess_stats.json

============================================================
处理统计:
  总文档数: 131,049
  保留文档: 130,926 (99.9%)
  总chunks: 1,281,646
  平均chunks/文档: 9.8

过滤统计:
  过短文本: 123
  标题质量: 33



准备进行embedding以及构建IndexFlatIP索引，因为chunk只有一百万且测试集中query数量少，所以使用IndexFlatIP取得最大精度
* 使用 HF backend + `embed_from_tokens` 可获得最高速度（前提：模型支持直接 id 输入）
* dtype：`float16`，device=`cuda`
* batch_size：尽量大（受显存限制，常见 1024~4096）
* 生成并保存：`id -> vector` 写入 FAISS（按批写入）

---

## 6) FAISS / Index（检索）

**结构**

* FAISS index: 存向量 + id
* metadata store (Parquet/SQLite/duckDB): id -> chunk_text, title

**Index 建议**

* faiss使用IndexFlatIP索引
* 建 index 时归一化（L2/cosine）: `index = faiss.IndexFlatIP(...)` with normalized vectors
* 写入时 batch commit + periodic persist
faiss索引只存储chunk_id和embedding，其他metadata通过chunkid从chunks的parquet文件中读取
faiss索引通过LZ4 算法压缩
你可以参考src\pipeline\build_embeddings.py，大部分代码都应该是相同的，主要有以下区别
1.faiss使用IndexFlatIP索引
2.embedding模型读取得不再是ids，而是从chunk中查询到开始结束的字符索引后，从documents_cleaned.parquet读取text
3.目前先采用 qwen3 0.6B embedding 模型而不是bge small，后续可能添加新的模型，建议模型获取和处理解耦，方便后续直接替换模型

1.我发现你这些模型参数不是读取yaml文件的不对吧，这些参数应该从yaml文件读，而我控制的是选择哪个模型，例如我传入qwen3，你就是使用 config[embedding_qwen3]的参数

1.yaml中参数的结构发生了变化，例如qwen现在是这样的
embedding:
  qwen3:
    model_id: "Qwen/Qwen3-Embedding-0.6B"
    max_length: 168
    device: null   # 自动选择 cuda/cpu（建议在有 GPU 时设为 "cuda"）
    dtype: float16 # 在 CUDA 上用 FP16 提升吞吐；CPU 将回退 FP32
    index_path: "data/faiss/qwen3_fp16_ip.faiss"
我建议将所有涉及model的参数放入yaml而不是硬编码
2.解决报错

embeding过程print信息，例如 
每秒处理多少chunks，
总共处理多少chunks/chunks总数(约128万),
已经处理的时间，
预计还需要多少时间
注意 \r 防止打印太多行
还有每次写入也要打印信息，注意别被 \r覆盖了，写入记得标注时间

embedding太慢了，我怀疑你在逐条处理，参考以下代码进行加速
三者组合后的速度大概如何？

典型效果：

方法	相对速度
原始 Python for-loop	1×
Arrow-based 拼接	3–6×
tokenizer multiprocessing / Fast tokenizer	4–10×
batched pipeline（避免逐条处理）	5–15×
三者叠加	30～80× 提升（真实可达）

如果你有几百万文本，这个差距是非常巨大的。

🧩 给你看一个最终的整合示例（核心结构）
import pyarrow as pa
import pyarrow.parquet as pq
from multiprocessing import Pool
from transformers import AutoTokenizer

tokenizer = AutoTokenizer.from_pretrained("your-model", use_fast=True)

def tokenize_batch(batch_texts):
    out = tokenizer(batch_texts, padding=False, truncation=False)
    return out["input_ids"]

def process_batches(all_texts, batch_size=2048):
    batches = []
    for i in range(0, len(all_texts), batch_size):
        batches.append(all_texts[i:i+batch_size])
    return batches

all_batches = process_batches(doc_texts)

with Pool(16) as p:
    tokenized_batches = p.map(tokenize_batch, all_batches)

tables = []
for batch_input_ids in tokenized_batches:
    arr = pa.array(batch_input_ids, type=pa.list_(pa.int32()))
    table = pa.Table.from_arrays([arr], names=["input_ids"])
    tables.append(table)

final_table = pa.concat_tables(tables)
pq.write_table(final_table, "tokens.parquet")


分析结果:
==================================================
平均CPU使用率: 7.1%
平均GPU使用率: 99.3%

🟢 结论: GPU瓶颈
   CPU准备数据速度够快
   建议: 增大batch_size、使用更大模型
会不会是因为CPU是单进程导致利用率上不去



我将代码转移到了linux服务器上，并安装了  faiss-gpu和 flash-attn，同时可以多线程，你通过yaml的use_linux参数控制是否导入和启动
注意，qwen3是支持 flash-attn的
在faiss的生成阶段，faiss-gpu的代码似乎并没有和faiss-cpu有什么区别，但在检索时，两者会有一定区别


我需要引入infgrad/Jasper-Token-Compression-600M模型，用于embedding，参数放在yaml的embedding部分中
使用例子可以参考/root/autodl-tmp/Science-Exam-LLM/src/download/test.py，不过为了精度我们只进行encode，不进行压缩
好像Jasper也是支持flash attn的

为什么qwen3_fp16_ip.faiss.lz4比qwen3_fp16_ip.faiss大这么多？他们难道不都是索引文件吗？难道qwen3_fp16_ip.faiss.lz4不是？
drwx---rwx 2 root root   48 Nov 23 12:03 checkpoints
-rw-r--r-- 1 root root  57M Nov 23 13:25 qwen3_fp16_ip.faiss
-rw-r--r-- 1 root root 4.9G Nov 23 13:25 qwen3_fp16_ip.faiss.lz4

会不会有一种可能qwen3_fp16_ip.faiss只是用来储存中间文件的，qwen3_fp16_ip.faiss.lz4储存的才是所有索引文件？

qwen3_fp16_ip.faiss.lz4有4.8G大概这个才是储存了索引的文件，128玩chunks的索引本来就应该有好几G，

D:\4000_projects\1project\LLM\data\faiss\qwen3_fp16_ip.faiss.lz4
帮我写一个脚本在src/script目录下，解压索引文件并对其进行检查，看是否有效，例如和chunks.parquet对比，看chunk_id有没有全覆盖

检查data\faiss\qwen3_fp16_ip.faiss是否是 qwen3_fp16_ip_chunk_ids.json，因为我储存时可能命名错误了，但我也不确定到底是不是，写一个脚本在src/script目录下，判断qwen3_fp16_ip.faiss到底是 一个索引，还是映射文件

这个映射文件是不是有问题啊，为什么不是映射data\processed\chunks.parquet，而是映射\data\raw\archive\25000_to_50000.parquet这种原始数据文件

我的映射文件好像没有写错，chunk id好像就是这样分的
[
  "0_to_25000.parquet:row:0:chunk:0",
  "0_to_25000.parquet:row:0:chunk:1",
  "0_to_25000.parquet:row:1:chunk:0",
  "0_to_25000.parquet:row:1:chunk:1",
  帮我写一个脚本，读取D:\4000_projects\1project\LLM\data\processed\chunks.parquet，前十行数据并打印

                             chunk_id                    doc_id              title  child_start  child_end  parent_start  parent_end  chunk_len
0  0_to_25000.parquet:row:0:chunk:0  0_to_25000.parquet:row:0       Becurtovirus            0        479             0         911        121
1  0_to_25000.parquet:row:0:chunk:1  0_to_25000.parquet:row:0       Becurtovirus          479        911             0         911         93
2  0_to_25000.parquet:row:1:chunk:0  0_to_25000.parquet:row:1       Cyprinivirus            0        403             0        1062        103
3  0_to_25000.parquet:row:1:chunk:1  0_to_25000.parquet:row:1       Cyprinivirus          403        891             0        1062 
parent_start固定为0，parent end 几乎以前，这个分块逻辑有问题啊，我的parent应该是差不多512token才对啊，ch

为我写一个脚本，名为check_retrieve,大概思路就是从chunk parquet随机读取出五个chunk的内容（需要拿idx去data\processed\documents_cleaned.parquet找text），然后拿qwen3 embedding然后检索，选出top5，查找对应text，打印出来，结果上理论上top1就是chunk本身，其他大概率也属于同一doc

check_retrieve_simple.py卡在Fetching 2 files:   0%|                                                                           | 0/2 [00:00<?, ?it/s]了




检索阶段，将Qwen3 0.6B embedding和BM25各搜200条，然后
**Paragraph Boosting**

* 如果多个 chunk 来自同一 paragraph/source, apply small bonus: `score += 0.05 * count_same_paragraph`
beta（段落增强系数）：0.05 ~ 0.15（越大越激进）
再通过RRF融合检索结果 k=60

使用

**MMR（去重）**

* 选取 topN 时用 MMR（λ=0.6~0.8）
λ（relevance vs diversity 权衡）: 0.6 ~ 0.8（越偏 1 越重相关性）

reranker topN 输入：N=80~200

最终选给 LLM 的 chunk 数 K_final：3~8（通常 3~5 最常用）
检索（Recall）

Dense: top 300~500  Qwen3 0.6B embedding

BM25: top 200  BM25

合并后 paragraph boosting：指数型 boosting

🔥 融合（RRF）

RRF k = 30~50

🔥 MMR（一定在 RRF 之后）

λ = 0.75~0.85

topN = 300（提供给 reranker）

🔥 Reranker（Cross-Encoder）

输入 200~300 条

使用最强的 cross-encoder（如 BGE-Reranker-large 或 Jina Reranker v2）

🔥 输出给 LLM

最终选 3~5 个 chunk（根据 reranker 分数）


src\retrieval\config.yaml不应该重新创建，新的参数应该放进项目目录config.yaml，
为什么src\retrieval\dense_retrieval.py不去复用src\embedding\embedding_qwen.py代码
记住，我用的是Qwen/Qwen3-Embedding-0.6B而不是 Qwen2
从config.yaml读取必要参数，并将参数 例如 RRF的k paragraph boosting等参数暴露在config.yaml

在test_retrieval_pipeline.py脚本中启用 reranker，注意
reranker需要重写，因为我换模型了，不再使用gguf量化，而是jinaai/jina-reranker-v3,其引用方式和gguf模型不同，因此你需要重写src\rerank\jina_reranker.py
from transformers import AutoModel

model = AutoModel.from_pretrained(
    'jinaai/jina-reranker-v3',
    dtype="auto",
    trust_remote_code=True,
)
model.eval()

Rank documents:

query = "What are the health benefits of green tea?"
documents = [
    "Green tea contains antioxidants called catechins that may help reduce inflammation and protect cells from damage.",
    "El precio del café ha aumentado un 20% este año debido a problemas en la cadena de suministro.",
]

# Rerank documents
results = model.rerank(query, documents)

# Results are sorted by relevance score (highest first)
for result in results:
    print(f"Score: {result['relevance_score']:.4f}")
    print(f"Document: {result['document'][:100]}...")
    print()
    
你的reranker部分肯定有问题，一次query要跑几十秒然后爆显存，这才0.6b模型

config = Config()
        reranker_config = config.get("reranker")
        self.device = reranker_config.get("device", "cuda" if torch.cuda.is_available() else "cpu")
        self.batch_size = reranker_config.get("batch_size", 16)
        self.use_parent_chunk = reranker_config.get("use_parent_chunk", True)


 cfg = Config()
        # Load model
        self.model = AutoModel.from_pretrained(
            model_id,
            dtype=dtype,
            trust_remote_code=trust_remote_code,
            batch_size=8
        )把参数暴露到 yaml 的reranker下
你的test_retrieval_pipeline.py是不是没有复用已有的检索pipeline？
分析性能瓶颈jina_reranker.py


1.BM25不再采用jieba分词器，而是使用
qwen:
  model_id: "ISTA-DASLab/Qwen3-8B-Instruct-FPQuant-QAT-MXFP4-TEMP"
也就是最后LLM的tokenizer
2.又出bug了

1.我发现ISTA-DASLab/Qwen3-8B-Instruct-FPQuant-QAT-MXFP4-TEMP用的tokenizer和Qwen/Qwen3-Embedding-0.6B是同一个，所以可以把Qwen3-Embedding-0.6B的tokenizer给BM25用
2.这是什么问题

先加载Qwen3-Embedding-0.6B，BM25的tokenizer直接从Qwen3-Embedding-0.6B引入，不要再次加载了

你是不是每次都是临时用bm25分词，这些结果应该要持久化放入data\processed
而且我需要你把bm25的逻辑从src\retrieval\retrieval_pipeline.py分离到src\retrieval\bm25.py，解耦
大概要存储这些数据
数据	必须？	作用
tokenized_chunks	✔	用于统计 TF 和 doc_len
chunk_len ✔	BM25 的
avgdl	✔	BM25 长度归一化
idf_table (DF)	✔	BM25 权重（IDF）
vocab / inverted_index	✔	高速查找哪些 chunk 包含某词
这些数据可以根据chunks.parquet 和 documents_cleaned.parquet得出，从chunks的index查找text再分词

我发现data\processed\bm25_index.pkl是pkl格式，帮我写一个脚本转化成 parquet格式，且修改bm25.py代码，让他以后生成 parquet格式的索引

不用考虑pkl格式，我以后只会使用parquet格式，另外我希望bm25把超参数暴露到项目目录下的config.yaml
我现在都不知道保存索引的路径时哪里决定的


现有的检索pipeline太慢了，按如下流程修改，注意模块化，一个文件代码最好不超过300行
Step 1）加载模型（embedding，LLM，reranker）

一次性加载，整个 RAG 过程中不卸载。

Step 2）dense 检索（embedding + Faiss）

Faiss On-Disk Index 或 Parquet mmap
→ 返回 topK chunck_ids

Step 3）BM25 检索（从 parquet 读取倒排表）

通过 term 查询倒排 postings，而不是全加载。

Step 4）paragraph boosting，RRF合并候选 topK，mmr去重后，取分数最高的 reranker_input_k =20个chunk id

Step 5）用DuckDB 从 Parquet 查询 chunk 文本

按 doc_id 批量读取对应文本
文本不常驻内存

Step 6）reranker

只对 top 20 文本做 rerank
然后 LLM 生成答案。
① Dense Embedding + Faiss ANN（On-Disk / mmap） → topK_dense

存成 .faissindex

mmap only

运行时内存占用非常小

② BM25 倒排表（压缩 pkl / mmap） → topK_bm25

索引结构：

idf_table: dict[str, float]

postings: dict[str, list[int]]

chunk_len: list[int]

全部序列化（统一 pkl 或多个 pkl）

运行时：

一次性 mmap 到内存

BM25 计算只使用倒排 list（极快）

③ Reranker 需要 chunk 文本 → DuckDB 读取 parquet

只在这一步使用 DuckDB，非常合理：

chunk_id → SELECT text/title/parent chunk

不加载整个文件

查询毫秒级

我对BM25持久化的索引的文件格式做出改变
文件	内容	建议格式	理由
chunk_len	每个 chunk 长度	parquet	简单标量，列式压缩极好
idf_table	token → idf	parquet 或 pkl	小字典，用 parquet/pkl 都 ok
inverted_index	token → postings	pkl ✔	postings 是 list，不规则结构
tokenized_chunks	每个 chunk 的 token 列表	pkl ✔（强烈）	列表嵌套结构
metadata	chunk_id, start, end, doc_id	parquet	结构化
这可能需要你写一个脚本读取D:\4000_projects\1project\LLM\data\processed\bm25_index目录下的文件进行转化，且修改BM25 save index的逻辑

我又做出改变了inverted_index tokenized_chunks我决定还是用parquet存储，因为parquet支持部分读取，现在不需要你写convert脚本，但需要你把BM25里的存储和读取代码改了
文件	内容	是否需要一次性读入内存？	原因
idf_table	token → idf(float)	✔ 必须全量读入（很小）	只几万 token 的字典，几 MB
chunk_len / avgdl	每个 chunk 的 token 长度	✔ 必须全量读入（较小）	BM25 公式必须用长度
tokenized_chunks	每个 chunk 的 token list	❌ 不需要全读（可 mmap 部分读）	只对 topK chunk 算 f(t,D)
inverted_index	token → posting 列表（chunk_ids）	❌ 按 token 查询即可，不要读全	只对 query token 用到
metadata	chunk → 内容（用于最终输出）	❌ 按 chunk_id 查询	输出答案时才用
示例（按 token 查倒排）：
import pyarrow.dataset as ds
ds_index = ds.dataset("inverted_index.parquet")
table = ds_index.to_table(filter=ds.field("token") == "apple")

对src\retrieval\retrieval_pipeline.py做出以下修改
1.query以batch形式传入
2.检索的每个步骤，都要分别计时，例如 dense检索和BM25检索，以及reranker，打印出 耗时以及处理的query数量
3.注意 使用的深度学习模型要 torch.no_grad() 模式，我怀疑reranker模型没有采用

检索时需要二次分batch，需要分别设计batchsize
模块	是否支持 batch	最佳 batch 大小	内存/显存耗费
Qwen Embedding	✔ 支持	通常 16–128	占显存（大）
Faiss	✔ 支持	越大越好（通过矩阵化加速）	中等
BM25	❌ 本质上不支持大 batch	1	占 CPU 内存（小）
Reranker（cross-encoder）	✔ 支持	极小 batch：1–8	显存占用非常大
另外我在config.yaml中将reranker的参数归于retrieval，这意味着reranker参数读取需要作出修改
总的来说，就是整个pipeline、qwen embedding、reranker有不同的batchsize
主要是对embedding的修改，因为reranker本来就是一个query一个query处理的
retrieval:
  # Dense Retrieval配置
  query_encoder:
    model_id: "Qwen/Qwen3-Embedding-0.6B"
    batch_size: 32
    max_length: 168
    device: null   # 自动选择 cuda/cpu（建议在有 GPU 时设为 "cuda"）
    dtype: float16 # 在 CUDA 上用 FP16 提升吞吐；CPU 将回退 FP32

  # Dense ANN 检索
  dense:
    batch_size: 64
    top_k: 600             # Dense检索的top-k（对128万库推荐600）
现在embedding的参数不从embedding:
  qwen3:
    model_id: "Qwen/Qwen3-Embedding-0.6B"
    max_length: 168
    device: null   # 自动选择 cuda/cpu（建议在有 GPU 时设为 "cuda"）
    dtype: float16 # 在 CUDA 上用 FP16 提升吞吐；CPU 将回退 FP32
    index_path: "data/faiss/qwen3_fp16_ip.faiss"
    读取，而是从检索部分的querty encoder读取，你可以自己改动使得更加合理
    外部自行决定query有多少，内部自己划分自己的，注意鲁棒性，处理query小于batchsize的情况

你怎么把embedding:
  qwen3:
    model_id: "Qwen/Qwen3-Embedding-0.6B"
    index_path: "data/faiss/qwen3_fp16_ip.faiss"
的一些参数给删除了，这是处理所有chunks生成索引的模型，和query encoder不共享参数，虽然我用的都是qwen3 embedding，但是逻辑上不共享，未来也可能有不同的

对src\script\retrieval\test_retrieval_pipeline.py做出改进，现在我需要能够控制传入query的数量，我要测试batch功能是否正常，以及整个pipeline的速度，可能可以简单的用现有query复制，但我也不确定会不会有缓存什么的干扰，你自己判断

我发现对整个chunks做BM25非常慢，所以我准备BM25 不要对整个库跑，只对 Dense 的 topK 再做 re-score

这是最强的 trick：

dense_top600 → 再做 BM25 重打分（rerank）
这样 BM25 只跑 600 条 chunk，非常快。
这意味着inverted_index.parquet	倒排索引	最大文件，速度最慢	丢弃 (节省空间)
倒排索引不再需要了

paragraph boosting仍然被需要，在RRF融合之后，输入MMR之前
RRF和MMR paragraph boosting也需要计时

注意现在reranker的输入数量由  
mmr:
    lambda: 0.8             # 相关性权重（0.8表示强调相关性，轻度去重）
    top_k: 20 
的top_k控制，我已经改好了，只是通知你
另外运行src\script\retrieval\test_retrieval_pipeline.py时，有关于reranker的warning 

你的修改方式错误，我的reranker已经归到retireval下了

BM25太慢了，你肯定是去整个读取 tokenized chunks 和inverted_index.parquet了，参考建议改进，由你决定是直接传入text在线分词，还是按id从磁盘读取tokenized chunks

src\script\retrieval\test_retrieval_pipeline.py报错了

我需要你以
qwen:
  model_id: "ISTA-DASLab/Qwen3-8B-Instruct-FPQuant-QAT-MXFP4-TEMP"
  device_map: "auto"
  max_new_tokens: 1           # 分类仅需 1 个 token（0 或 1）
  gen_temperature: 0.0        # 关闭采样
  do_sample: false
  trust_remote_code: true
做一个答案生成模型，可以zeroshot回答，也可以提取hidden state 连接MLP进一步LORA，具体代码放在modeling里。
首先从读取D:\4000_projects\1project\LLM\data\raw\kaggle-llm-science-exam\test.csv问题及回答，将query传到检索的pipline里，拼接成context，注意是批处理的，先生成所有的context，然后加载LLM，开始推理，然后生成zero shot的pipeline，让我作为基线，prompt可以参考
# System Message (可选，取决于模型是否支持)
SYSTEM_PROMPT = "You are a scientific expert answering multiple-choice questions based strictly on the provided context."

# User Prompt Template
LISTWISE_PROMPT = """
### Instructions:
You are participating in a high-level science exam.
Your task is to answer the following multiple-choice question.
You MUST derive your answer primarily from the provided "Context" sections.
If the answer is not directly in the context, use your scientific knowledge to deduce the most plausible answer consistent with the context.

### Context:
{context}

### Question:
{question}

### Options:
A: {option_a}
B: {option_b}
C: {option_c}
D: {option_d}
E: {option_e}

### Task:
1. Analyze the context and the question.
2. Evaluate each option (A, B, C, D, E) one by one. Explain why it is correct or incorrect based on the context.
3. Select the best single answer.

### Output Format:
Provide your reasoning first, then conclude with the final answer in the format: "Answer: X" (where X is A, B, C, D, or E).
"""

一个问题生成答案几分钟都没出结果，我记得之前zeroshot是可行的，会不会是因为要输出的token天长了
我建议修改prompt，并限制LLM的输入为1个token，只能输出答案，例如
另外我要求参数暴露在config.yaml中，包括验证集路径
qwen:
  model_id: "ISTA-DASLab/Qwen3-8B-Instruct-FPQuant-QAT-MXFP4-TEMP"
  device_map: "auto"
  max_new_tokens: 1           # 分类仅需 1 个 token（0 或 1）
  gen_temperature: 0.0        # 关闭采样
  do_sample: false
  trust_remote_code: true

  1.src\script\generation\zeroshot_pipeline.py中，要求参数统一从config.yaml读取，而不是命令行
  2.另外模型到底为什么这么慢，是因为你存到cpu上了吗
  3.src\modeling\qwen_generator.py的extract_hidden_states怎么这么复杂，我记得提取隐藏层，只需要模型的一行设置就行了

参考md使得LLM仅仅输出一个 ABCDE中的字母，而不是<think>这种

因为512token的父chunk对于后续太大了，我需要设计一个脚本，使得父chunk不超过256token，从chunks.parquet中读取数据，并从documents_cleaned.parquet读取text，根据char对应的256token数，重新调整parent 的start 和end索引，注意不要生硬chunk，注意语义单元的完整性，分割处
我发现你的父chunk生成逻辑有问题，而且我想调整父chunk的大小了（从512到256），但我不能调节子chunk的划分，因为一旦调节就需要重新embedding时间太长，所以你额外写一个生成父亲chunk的脚本，放在src\chunking目录下，可以根据以下规则生成
专业 RAG 系统的父 chunk（“Parent Document”）要求：

与文本原本语义高度对齐

256 tokens 尽量对应自然段落

不随 child chunk 滑动变化

1 个父 chunk 覆盖多个子 chunk（通常 3~6 个）

LLM 看到的父 chunk文本应该自然、不碎、不重复

而你的版本：

每个子 chunk 都会产生一个局部父 chunk → 20x 重复

切割不避开句号/换行

父 chunk 不等于语义单元

不节省上下文 window

不利于 rerank / LLM reading / context cohesion

所以——
👉 你的 chunking 方法不能用于真实的 SOTA 2025 Parent Document Retriever。

✅ 正确的做法（你应该修改成的逻辑）
正确划分流程应该是两层分离：
第一层：按语义单位切父 256 tokens）

独立于子 chunk 运行！

规则：

使用断句器（或 tokenizer 后标点）

聚合句子直到约 512 tokens

切割点必须靠近句号、换行等

父 chunk 数量应远远少于子 chunk
算出了每一个父chunk的字符索引范围，再从对应doc里的chunks里查，调节chunks里的索引


Blingfire 做句子切分（推荐用于你的任务）,而不是for punctuation in ['.', '?', '!', '\n', '。', '！', '？', ';', '；']:
🔥 优点

极快（比 spaCy 快 10～50 倍）

纯 C++ 核心，Python 调用开销极小

已经内置 sentence tokenizer（不需要自己写 regex）

不会像 regex 一样误切 “Dr.” “e.g.” 等缩写

📌 示例代码（你可以直接放进 ParentChunker）
import blingfire

def split_sentences_blingfire(text: str):
    # 使用 Blingfire 内置的句子切分器
    # 每个句子一行
    s = blingfire.text_to_sentences(text)
    return [sent.strip() for sent in s.split("\n") if sent.strip()]

首先父chunks应该保存为parquet
其次，我要的不是一个映射表，而是直接修改chunks里的父chunk字符索引
要小心，这里面的索引时基于data\processed\documents_cleaned.parquet里面的text的，这意味着为了一致性，在生成父chunk的过程中，不能对text修改
在判断父chunk和子chunk是否包含时，可以参考
hild→parent 映射时的更稳健算法（替代中心点法）

你现在用 child_center 与最大 overlap，如果你担心边界，改为：

def map_child_to_parent(parent_chunks, child_positions):
    mapping = []
    for cstart, cend in child_positions:
        # 首选：完全包含
        found = False
        for pidx, p in enumerate(parent_chunks):
            if p['start_char'] <= cstart and cend <= p['end_char']:
                mapping.append(pidx); found=True; break
        if found: continue
        # 次选：最大 overlap
        best, best_ov = None, 0
        for pidx, p in enumerate(parent_chunks):
            overlap = max(0, min(cend, p['end_char']) - max(cstart, p['start_char']))
            if overlap > best_ov:
                best_ov = overlap; best = pidx
        mapping.append(best if best is not None else -1)
    return mapping
最后统计父子chunk的信息，例如数量，多少比例子chunk完全被包含，平均每个父chunk包含几个子chunk这种

子chunk进行切分的时候没有使用tokenizer，所以


进行数据处理，对 data\raw\6000_train_examples.csv按照 0.9训练集，0.1验证集划分，同时加上data\raw\kaggle-llm-science-exam\test.csv作为验证集